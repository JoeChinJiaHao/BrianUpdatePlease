package com.example;

public class TryCatchFinally {
    //sample code for try cath with finally
    //try without catch is a try with resource (normally for non-exception throwing items)
    //if using input-outputstream, exception is to be expected
    //finally is ganuranteed to run
}
